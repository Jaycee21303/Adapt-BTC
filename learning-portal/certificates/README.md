Certificates are rendered dynamically on the client canvas and downloaded directly by the user.
Static template images are intentionally omitted to keep the repository binary-free. The
certificate-engine.js script draws the branded frame, text, and signature styling at runtime.
